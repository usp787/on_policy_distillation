# PINNED to a known-consistent set. Unpinned floors on Python 3.11 pulled
# bleeding-edge (torch 2.11/cu130, trl 1.5, transformers 5.x) that breaks vLLM
# and the trl 0.x GKD/GRPO APIs the scripts use. These exact versions are the
# set pip resolved together (and match the cuda/12.8.0 = cu128 module).
# To bump later: change a pin, re-run setup, confirm Phase 0 still works, re-pin.
torch==2.8.0              # cu128 build; vLLM 0.11 is coupled to this
transformers==4.57.6      # Qwen3 OK; trl 0.24 API expects 4.x (NOT 5.x)
trl==0.24.0               # GKD in trl.experimental.gkd; GRPO vllm colocate
peft==0.17.1
accelerate==1.10.1
datasets==4.5.0
vllm==0.11.0              # Qwen3 + FP8 on Hopper; pins torch 2.8.0/cu128
math-verify               # answer checking for the RLVR reward (needs py>=3.10)
pyyaml>=6.0               # config loading
pillow                    # torchvision (pulled by torch) imports PIL eagerly; vLLM's
                          # transformers image-processor import needs it even for text models
# flash-attn is optional (slow compile, sdpa fallback). Installed separately in
# env/setup_env.sbatch; if its build fails, transformers uses sdpa automatically.
