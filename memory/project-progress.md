---
name: project-progress
description: on_policy_distillation — phase-by-phase progress tracker (where to pick up)
metadata:
  type: project
---

Progress on the on_policy_distillation project (as of 2026-06-21):

- ✅ **Env setup** — conda 3.11 env, pinned stack, scratch HF cache. See [[cluster-env-setup]].
- ✅ **Data prep** — `data/train_math.json` re-pulled from `agentica-org/DeepScaleR-Preview-Dataset` (~4k competition-math prompts; the original 746 HuggingFaceH4/MATH set was too easy → GRPO reward saturated at 1.0 → zero advantage), `data/aime24.json`, `data/aime25.json` (30 each).
- ✅ **Phase 0 baseline** — Ruler = 8192 tokens. 4B = **51.67% combined**, 8B = 21.67%. See [[token-budget-and-baseline]] (8B non-thinking is a *worse* teacher than the student).
- ✅ **Phase 1 — RLVR/GRPO — DONE, NULL RESULT.** DeepScaleR gave real reward variance/gradient (vs MATH which gave none), but no AIME gain. avg@4 (k=4, temp 0.7, 8192): baseline **50.00** combined → Phase-1 **48.33** (−1.67 ≈ 1 question/60, within noise; subsets move opposite directions = noise signature). **Finding: `Qwen3-4B-Instruct-2507` is already fully RL-post-trained on math; naive single-reward LoRA-GRPO adds nothing and can mildly shift the policy *away* from its optimized state.** Accepted as the finding, not chasing a within-noise gain. Adapter at `checkpoints/phase1_grpo` (no longer needed as a teacher — self-distill plan dropped).
- ✅ **Phase 2 — distill from `Qwen/Qwen3-30B-A3B-Instruct-2507` (GKD) — DONE (step 300), FLAT/NULL RESULT.** Final model (step-300 adapter, **FP8** teacher) avg@4 (k=4, t0.7, 8192) = **48.75% combined** (aime24 53.33 / aime25 44.17) vs baseline **50.00** → −1.25, within noise. So OPD did **not** lift the student despite the teacher's real +14 gap. Likely **dominant** cause (per [[opd-failure-paper]], arXiv 2604.13016): the 30B is a **same-recipe, same-family scaled sibling** of the 4B (both 2507-Instruct, non-thinking) → it carries little *genuinely new knowledge* to transfer, so the +14 benchmark gap is "better fit to the same data," not a learnable signal (their OPD condition (ii); matches their "strong teacher → ~5% gap-recovery" fingerprint). Secondary confounds we control less for: the **2048 train-cap vs 8192 eval-ruler** mismatch + LoRA r32. Note: dropping SFT was **not** the core miss — their SFT cold-start fixes *thinking-pattern* mismatch (condition (i)), which our same-family pair already satisfied. The fix that let it finish: **FP8 teacher** (bf16 OOM'd at step 138 — the dense full-vocab KL tensor had no room beside the 61 GB bf16 teacher) + batch4/accum2 + auto-resume chained across ~8 slots (ckpt-75→100→125→175→225→250→275→300, job 7750856 closed it out). ⚠️ The committed **greedy** `phase2_checkpoints_phase2_distill.json` (43.33) is STALE — it's the attempt-1 *intermediate* checkpoint with the *bf16* teacher (job 7495022), NOT the final model; compare only the avg@4 number. README §12 + §10 updated (2026-06-23) to mark Phase 2 done + flat. Teacher details: 30B MoE (3.3B active), non-thinking, AIME25 61.3 vs student 47.4 (+14 gap), no SFT, vocab matched (logit KL valid — confirmed). History (the two failed attempts before it finished):
  - **Attempt 1 (100 steps) — badly UNDER-TRAINED**: epoch 0.2, ~16 min of an 8h slot, loss still falling → student perturbed but hadn't absorbed the teacher; greedy combined fell to 43.33 (vs baseline 51.67 greedy / 50.00 avg@4). This is NOT evidence SFT is needed — the instruct student is already in-support (SFT's role is base→support only; that test would need Qwen3-4B-Base).
  - **Attempt 2 (600 steps, lr 5e-6, max_new_tokens 4096, micro-batch 2 + grad-accum 4) — TIMED OUT, zero output (job 7497131).** Not an OOM (the expandable_segments + micro-batch fix held). It hit the 8h wall at **step 59/600**. TRL GKD samples on-policy rollouts with HF `generate` (no vLLM, README Appx B) → O(tokens) sequential decode that runs to the cap when no EOS; per-step time was 40–80× the "~10–15 s/step" estimate (10–30 min/step), worst-case dominated by full-4096 rollouts. Compounded by: 4096 cap (2× worst case), micro-batch-2 generating 4× per logged step on tiny batches (awful decode parallelism), and lr 5e-6 lengthening rollouts. **Critical:** `save_steps=100` > 59 → no checkpoint ever written, eval never ran → the only artifact is the `.err`. Losing a full 8h slot to nothing is the real lesson.
  - **Re-tuned config (A+B, 2026-06-09, in repo) for the next run.** A (cut generate cost): max_new_tokens 4096→**2048** (back to attempt-1's proven no-OOM batch-8 profile; accepted trade-off = short-bias vs the 8192 eval ruler, the unavoidable cost of HF-generate — vLLM `src/handrolled_opd.py` is the escape if 2048 transfer is too weak), per_device_train_batch_size 2→**8** + grad-accum 4→**1** (one generate/step), max_steps 600→**300** (~0.6 epoch). B (survive timeout): save_steps 100→**25** + `save_total_limit: 2`; `train_gkd.py` auto-resumes from last `checkpoint-N` via `get_last_checkpoint`; sbatch has `--open-mode=append` → just `sbatch slurm/phase2_distill.sbatch` again to chain across slots. lr kept 5e-6 (drop to 3e-6 if grad_norm spikes >~10 or step time creeps). **Watch first few steps: it/s must be tens of seconds, not 600–1200 s/it.** If avg@4 still ≤ baseline after a *finished* run, then question whether LoRA r32 / 4B capacity caps the transfer.
- ❄️ **Phase 3 — FROZEN.** Parked while Phase 2's 30B teacher carries the reproduction. (235B-A22B-2507 scores higher but won't fit one H200 even FP8; a revisit means thinking-mode or multi-GPU.) Discarded options: 8B (worse than student in non-thinking), 14B/32B April hybrids (no non-thinking math edge).

Watch-items: GKD import path is `trl.experimental.gkd` (train_gkd.py has a top-level fallback). Eval auto-merges LoRA adapters into a `<ckpt>_merged` full-model dir before vLLM (eval_aime.py); these ~8GB merged dirs now write under `$HF_HOME/opd_merged` on **scratch** (storage rule), not the home node. Still reclaim Phase-1's `checkpoints/phase1_grpo_merged` (~8GB) + intermediate `checkpoint-*/` on the home node; the small final adapter is the keep-worthy artifact.
